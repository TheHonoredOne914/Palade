# Dependencies
node_modules/

# Build outputs
dist/
exports/

# Logs & Environment
*.log
.env

# OS / Editor Artifacts
.DS_Store
Thumbs.db
*.swp
.tmp*

# Palade Engine
.palade/
.paladeignore
palade-badge.svg

# Development & Testing Configs (palade.config.ts holds API keys — never commit)
palade.config.ts
palade.targets.ts
palade.agents.ts
sample-report.html
test-project/
scratch/

# Local project-management notes (not shipped)
RELEASE_OVERVIEW.md
TECHNICAL_REFERENCE.md

# graphify: the repo SHIPS its knowledge graph (graph.json, GRAPH_REPORT.md,
# wiki/, graph.html) so those stay tracked. Only the transient regeneration
# artifacts are ignored — dated run snapshots and the AST cache.
graphify-out/cache/
graphify-out/20*/

# Note: CLAUDE.md and the shipped graphify-out/ files are intentionally tracked
# (the repo ships its own knowledge graph and agent guide) — do not ignore them.
